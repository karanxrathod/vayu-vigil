# Server Configuration
PORT=3001
JWT_SECRET=supersecrethackathonkey2026
NODE_ENV=production

# Database Configuration (WASM SQLite recommended for zero-config hackathon reliability)
DATABASE_URL=sqlite://./vayu_vigil.db

# Background Service Timers (in milliseconds)
SIMULATOR_INTERVAL_MS=10000
SCORING_INTERVAL_MS=30000

# Google Cloud & AI Configuration (Required for Live Inference Judging Criteria)
# Provide your Gemini 1.5 Flash API key from Google AI Studio / Vertex AI
GEMINI_API_KEY=your_gemini_api_key_here
MOCK_MODE=false

# Alert Webhook & Notifications
WEBHOOK_LOG_ONLY=true
